syntax = "proto3";

// 定义包名
package action;
option go_package = "./enum/action";


enum Action {
  //Redirect | Qrcode | Prepay
  Action_UNKNOWN = 0;
  Redirect = 1;
  Qrcode = 2;
  Prepay = 3;
  // 无回调的跳转支付，拉卡拉私有协议版
  RedirectNOBack = 4;
}