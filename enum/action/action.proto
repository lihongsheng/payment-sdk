syntax = "proto3";

// 定义包名
package action;
option go_package = "./enum/action";


enum Action {
  // 未知
  Action_UNKNOWN = 0;
  // 跳转支付
  Redirect = 1;
  // 跳转二维码
  Qrcode = 2;
  // 预支付
  Prepay = 3;
}
